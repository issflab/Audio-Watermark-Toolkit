numpy==1.26.4
scipy==1.12.0
soundfile==0.13.0
pandas==2.2.3
PyYAML==6.0.2

# AudioSeal + torch/torchaudio (install torch/torchaudio per your CPU/CUDA)
audioseal

# PESQ (optional; for quality scores in demo/eval)
# On Windows, building may require: Microsoft C++ Build Tools
# If install fails, pipeline still runs; PESQ scores will be N/A
pesq

# MP3 and format conversion (run_demo, attacks); requires ffmpeg on PATH
pydub
